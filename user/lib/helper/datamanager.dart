// Firebase removed: this file previously provided a MockDocumentSnapshot shim
// that adapted a JSON map to a Firestore DocumentSnapshot. The data layer now
// uses the `fromJson(Map)` factories directly, so the shim is no longer needed.
